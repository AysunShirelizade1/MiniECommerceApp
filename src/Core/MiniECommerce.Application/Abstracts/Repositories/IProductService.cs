using MiniECommerce.Application.Common;
using MiniECommerce.Application.DTOs.ProductDto;

namespace MiniECommerce.Application.Services.Interfaces
{
    public interface IProductService
    {
        //Task<IEnumerable<ProductDto>> GetAllAsync(ProductFilterDto filter);
        //Task<ProductDto?> GetByIdAsync(Guid id);
        //Task<ServiceResult<ProductDto>> CreateAsync(ProductCreateDto dto, Guid ownerId);
        //Task<ServiceResult<ProductDto>> UpdateAsync(Guid id, ProductUpdateDto dto, Guid ownerId);
        //Task<ServiceResult<bool>> DeleteAsync(Guid id, Guid ownerId);
        //Task<IEnumerable<ProductDto>> GetByOwnerIdAsync(Guid ownerId);
    }
}
