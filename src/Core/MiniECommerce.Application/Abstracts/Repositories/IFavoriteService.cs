using MiniECommerce.Application.Common;
using MiniECommerce.Application.DTOs.ProductDto;

namespace MiniECommerce.Application.Services.Interfaces
{
    public interface IFavoriteService
    {
        //Task<ServiceResult<bool>> AddToFavoritesAsync(Guid userId, Guid productId);
        //Task<ServiceResult<bool>> RemoveFromFavoritesAsync(Guid userId, Guid productId);
        //Task<IEnumerable<ProductDto>> GetFavoritesByUserIdAsync(Guid userId);
    }
}
